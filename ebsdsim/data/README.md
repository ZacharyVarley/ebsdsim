# Packaged assets

| Path | Contents |
|---|---|
| `lightweight_hist_surrogate.npz` | Default TinyHistNet energy-surrogate weights |
| `preset_cifs/` | Bundled CIF presets (`Ni.cif`, `GaN.cif`, …) |
| `goldens/` | CPU golden arrays used by tests |

Generated Python tables are *not* here — see `ebsdsim/crystal/_generated/`.
